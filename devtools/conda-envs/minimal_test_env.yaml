name: test
channels:
  - conda-forge
  - defaults

dependencies:

  # core
  - python
  - pip

  # qcstack
  - qcelemental>=0.26

  # deps
  - numpy
  - scipy >= 1.9
  - tqdm

  # testing
  - pytest >= 4.6.4
  - pytest-cov
  - pytest-xdist


  # Pip-only installs
  - pip:
      - msgpack>=1.0
